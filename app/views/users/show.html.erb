<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% unless @user.microposts.empty? %>
  <p>
    First micropost by <%= @user.name %>:
    <strong>Content:</strong>
    <%= @user.microposts.first.content %>
  <p>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
